export enum EtiquetaNoEncontrada {
    DESCONOCIDA = "desconocida",
}

export enum EstadoPanelDashboard {
    ACTIVO = "activo",
    BASE = "base",
    EN_PROGRESO = "en_progreso",
}

export enum AlcanceGaleriaImagen {
    GALERIA = "gallery",
    N8N = "n8n",
    OPTIMIZADA = "optimized",
}

export enum ModoCalidadImagen {
    BALANCEADO = "balanced",
    ALTA = "high",
}

export enum TamanoPaginaGaleria {
    DIEZ = 10,
    VEINTICINCO = 25,
    CINCUENTA = 50,
}

export enum ColeccionOrigenImagen {
    GALERIA = "gallery",
    N8N = "n8n",
    OPTIMIZADA = "optimized",
    LOCAL = "local",
    ORIGINAL = "original",
    DESCONOCIDA = "desconocida",
}

export enum OrigenFlujoImagen {
    GALERIA = "gallery",
    N8N = "n8n",
}

export enum PeriodoFacturacion {
    SIETE_DIAS = "7d",
    TREINTA_DIAS = "30d",
    NOVENTA_DIAS = "90d",
}
